#!/bin/bash
if [ "$1." = "-?." ]; then
  cat <<EOT

$(basename $0) <format> <files>

Uses LibreOffice to convert the files to the format specified. Typical examples:

  $(basename $0) xlsx *.ods.xml
    Convert all Open Office spreadsheets into Excel XML format.

  $(basename $0) xml *.xlsx
    Vice-versa: convert Excel spreadsheets into Open Office XML format.

EOT
else
  cd /workspace
  # TODO: deal with filenames with spaces
  soffice --headless --convert-to $1 $2
fi
